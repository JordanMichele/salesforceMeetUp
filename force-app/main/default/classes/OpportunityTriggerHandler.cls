public class OpportunityTriggerHandler extends TriggerHandler {
    // Setting up the name to be send over to the TriggerHandler
    private static final String TRIGGER_NM = 'Opportunity Trigger';
	public OpportunityTriggerHandler() {
    // Calls the super class (Trigger handler) and passes in the name of the trigger
        super(TRIGGER_NM);
    }
}